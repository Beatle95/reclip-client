module;

#include <memory>

#include "core/clipboard_observer.h"

export module core.clipboard;

import base.observer_helper;

namespace reclip {

export class Clipboard : public SimpleObservable<ClipboardObserver> {
 public:
  static std::unique_ptr<Clipboard> Create();

  virtual ~Clipboard() = default;
  virtual void WriteText(const std::string& text) = 0;
  virtual void Start() = 0;
  virtual void Stop() = 0;
};

}  // namespace reclip
